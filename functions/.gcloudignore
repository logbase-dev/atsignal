# This file specifies files that are *not* uploaded to Google Cloud
# using gcloud. It follows the same syntax as .gitignore

.gcloudignore
.git
.gitignore
node_modules/
.env*
*.log
.DS_Store
*.tsbuildinfo
*.map
# Note: lib/ is needed for deployment, so don't exclude it


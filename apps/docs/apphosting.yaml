# Firebase App Hosting configuration for docs app
runtime: nodejs20

buildCommand: cd ../.. && npm install && npm run build:docs
runCommand: npm start

env:
  - variable: NODE_ENV
    value: production
